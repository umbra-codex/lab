apiVersion: v1
kind: Service
metadata:
  name: mealie
  labels:
    app: mealie
  namespace: mealie
spec:
  ports:
    - port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
    pod: mealie
  type: LoadBalancer
